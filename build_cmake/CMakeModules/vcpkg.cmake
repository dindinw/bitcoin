if (NOT "$ENV{VCPKG_TRIPLET}" STREQUAL "")
    set(BOOST_ROOT    $ENV{VCPKG_TRIPLET})
    set(OPENSSL_ROOT_DIR $ENV{VCPKG_TRIPLET})
    set(LIBEVENT_ROOT_DIR  $ENV{VCPKG_TRIPLET})
    set(LIBDB_CXX_DIR  $ENV{VCPKG_TRIPLET})
    set(ZeroMQ_DIR $ENV{VCPKG_TRIPLET}/share/zeromq/)
endif()